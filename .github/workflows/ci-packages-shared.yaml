name: ci-packages-shared

on:
    push:
        branches: [main]
        paths:
            - "packages/shared/**"
    pull_request:
        branches: [main]
        paths:
            - "packages/shared/**"

jobs:
    ci:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout code
              uses: actions/checkout@v4

            - name: Setup Node.js
              uses: actions/setup-node@v4
              with:
                  node-version: 24
                  cache: "npm"

            - name: Install dependencies
              run: npm ci

            - name: Run ESLint
              run: npm run lint:shared

            - name: Run Tests
              run: npm run test:shared

            - name: Build TypeScript
              run: npm run build:shared
